# Acceptance Records

These reports summarize accepted software evidence. Their machine-readable
counterparts live under `artifacts/baselines/`. Normal tests write to temporary
directories and ordinary acceptance runs write to ignored `artifacts/runs/`.

No report in this directory proves physical hardware behavior unless it cites
real installation evidence explicitly.
