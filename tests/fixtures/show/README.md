Phase 17 show fixtures live in `config/acceptance/show-orchestration-v1/show.yaml` and
`config/acceptance/show-orchestration-v1/layout.yaml` so the existing `validate-show` command can
load the show path directly.
