# Archived Agent Campaigns

These manifests and task files describe completed campaigns. Manifests point
to their archived task locations, but they are never selected by default;
running one requires an explicit `--manifest` path. Historical task content
otherwise retains the commands and contracts recorded at the time.

`.agent/tasks/TEMPLATE.md`, prompts, and schemas remain the reusable active
automation surface.
